# Implementation Plans

Generated by the improve skill on 2026-06-12 at commit `2408b3e` (branch `master`). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

> **Selection note**: this audit ran non-interactively, so plans were written for the top findings by leverage (impact ÷ effort, weighted by confidence), per the skill's default. The full findings list — including audited-but-unplanned items — is below; ask for additional plans from the backlog at any time.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Fix broken line-item-claims query in settlement summary (+ N+1) | P1 | M | — | MERGED (2026-06-17 into `master`, merge `ac3e94a`) |
| 002  | Close cross-trip IDOR holes (itinerary, lodging, photo reactions) | P1 | M | — | MERGED (2026-06-17 into `master`, merge `887a5c2`) |
| 003  | Router test baseline for expenses + planning | P1 | L | 001, 002 | MERGED (2026-06-17 into `master`, merge `6279ec3`) |
| 004  | Enforce trip status state machine server-side | P2 | S | — (coordinate with 003 on trips.test.ts) | MERGED (2026-06-17 into `master`, merge `fadcd98`; map gained `planning→en_route` for the shipped Start Trip flow) |
| 005  | Clear high-severity transitive dependency advisories | P2 | M | — | MERGED (2026-06-17 into `master`, merge `7c71084`; follow-up fix `0a95861` re-scoped the `h3` override to `h3@1` so nitro@3 keeps h3 v2 — an unscoped `>=1.15.11` broke the `@sortey/tanstack-start` build) |
| 006  | Truth-sync docs/ai/STATUS.md + README title | P2 | S | — | MERGED (2026-06-17 into `master`, merge `1e93817`) |

Status values: TODO | IN PROGRESS | DONE | MERGED | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

> **Consolidation note (2026-06-17)**: all six plans were merged into `master` (`--no-ff`) and the per-plan `advisor/*` worktrees + branches were removed. A full `pnpm build` after the merge caught two breaks, both fixed in `0a95861`: (1) plan 005's `h3` override (above), and (2) a *pre-existing* client/server boundary bug — `@sortey/realtime`'s barrel re-exported the client-only `useTripChat`/`useTripLocations` hooks into the server-side `expenses.ts` router; fixed with a server-safe `@sortey/realtime/event-log` subpath export. Verified green: typecheck 41/41, build 23/23, `@sortey/api` 213 tests, `@sortey/realtime` 36 tests.

## Dependency notes

- 003 follows 001 and 002 because 001 establishes the `SettlementStore` harness pattern 003 extends, and all three add files to `packages/api/src/router/__tests__/` (merge-conflict avoidance, and 003's characterization tests should capture *fixed* behavior).
- 004 and 003 both touch `packages/api/src/router/__tests__/trips.test.ts` — don't run them concurrently.
- 001, 002, 004, 005, 006 are otherwise independent and parallel-safe.
- The deferred "split trips.ts" refactor (backlog below) must not start before 003 is DONE.

## Audited findings not planned (backlog — ask for a plan if wanted)

| Finding | Category | Impact | Effort | Notes |
|---------|----------|--------|--------|-------|
| `expenses.list` returns all rows, no pagination (`packages/api/src/router/expenses.ts:145-172`) | perf | MED | S | `chat.ts` has the 50-row precedent (`MAX_HISTORY_LIMIT`) |
| Money + status formatting duplicated across apps (19 files with `toFixed(2)`; `statusColor` re-implemented in ≥4 nextjs pages); `packages/validators` is an explicit stub | tech-debt | MED | M | Extract shared `formatCurrency`/status maps; matters more once non-USD currencies are real |
| `trips.ts` god file (1,757 lines: CRUD + invites + segments + members + share links + domain functions) | tech-debt | MED | M | Split into sub-routers — **only after plan 003** |
| `/demo` dashboard (`apps/nextjs/src/app/demo/_components/app-shell.tsx`, 6,171 lines) — IMPLEMENTATION_PLAN Phase 6 says wire live data then delete `/demo` | tech-debt | LOW-MED | S | Verify nothing imports it, then delete per the plan's own intent |
| No DB-backed integration tests (in-memory store harness tests logic, not SQL); test DB URL hardcoded per-file (`postgresql://...gmacko_test` in test headers) | tests | MED | M | A real-Postgres vitest project would have caught the plan-001 bug directly |
| Idempotency keys exist on settlements but not on expense/poll/proposal creation | correctness | LOW-MED | M | Design question (client retry semantics) before schema work |
| Trip-table RLS policies not yet enabled (tenant.ts builders exist) | security | MED | L | Defense-in-depth under plan 002's app-level checks |

## Direction findings (product options, evidence-grounded — design/spike plans on request)

1. **Wire the existing OCR pipeline into expenses.** `packages/api/src/ocr/` contains Claude + Gemini extractors, a reconciler, fixtures, and tests — and zero references from any router. The receipt→expense flow is the product's stated Phase 3 centerpiece (IMPLEMENTATION_PLAN); the remaining work is one mutation + review UX. Strongest cheap win in the codebase. (Coarse effort: M.)
2. **Road-trip core loop: Side Trip / Fuel Zone / Route Gradient / Predicted Stop.** Fully specified vocabulary in `CONTEXT.md:54-77` and ROAD_TRIP_PROPOSAL; schema has route polylines per segment; none of the four exist in code. This is the van-life differentiator the docs promise. (Coarse effort: L, multi-feature.)
3. **Corridor search data + UI.** `corridor.ts` router and the PostGIS ADR exist, but there's no POI importer (iOverlander/Recreation.gov named in the proposal) and no surface in either app. API-first foundation is in place. (Coarse effort: L.)
4. **SMS invites.** Full design docs exist (`docs/ai/A2P_10DLC_REGISTRATION.md`, `docs/plans/2026-06-04-phase1a-texted-invites.md`); external 10DLC approval is the long pole, so starting registration early is cheap. (Coarse effort: M-L plus external lead time.)

## Findings considered and rejected

- **Push notifications "scaffolded but not integrated"** (audit subagent claim): wrong — `push_token` table, `notifications.registerPushToken`/`unregisterPushToken`, and a working Expo push sender (`packages/api/src/notifications/send.ts`) all exist and routers call it. Not a finding.
- **`effect` AsyncLocalStorage advisory "directly affects auth/realtime context"** (subagent framing): overstated — `effect` is reachable only inside uploadthing's tree, not the app's auth or DB runtime. Kept in plan 005 as hygiene with honest framing.
- **Haversine recomputation in driving-summary convoy loop**: micro-optimization, O(members) trig per render; not worth a plan.
- **drizzle-orm version lag**: false alarm — the subagent's own check showed the pinned version is current.
- **PostGIS raw `sql` tagged templates**: by design per `docs/adr/0001-postgis-for-spatial-queries.md`; only unsafe interpolation would be a finding, and none was found.
- **Billing/metering scaffold unused**: intentional template layer kept thin on purpose (README/template docs); recommend a one-line note in IMPLEMENTATION_PLAN that it's future-option, nothing more.
- **Dev auth bypass routes** (`/api/dev/auto-login`, `/api/dev/last-magic-link`): intentional for local/E2E per STATUS.md; no evidence found of production reachability during this audit (not exhaustively verified — flag for the next security pass to confirm build-time exclusion).
